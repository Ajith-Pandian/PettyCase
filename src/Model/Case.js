import moment from "moment";

export default class Notes {
  constructor(
    scid,
    constituents,
    description,
    time = new Date(),
    isFavorite = false,
    isHearted = false,
    isPoem = false,
    isStory = false
  ) {
    scid,
      constituents,
      flags,
      info,
      keywords,
      newsTag,
      rationale,
      segments,
      stats,
      updates,
      version;
  }
}
